{
  "name": "ai-health-record-translator",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "npm --prefix client run preview"
  }
}
